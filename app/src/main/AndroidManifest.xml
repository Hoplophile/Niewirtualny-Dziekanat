<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.piotr.niewirtualnydziekanat">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAyQW-wguLXBk0n-nDQOw_jvkgykFwvsKQ"/>
        <activity
            android:name=".MainActivity"
            android:label="Niewirtualny Dziekanat"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity><activity
            android:name=".DeaneryActivity"
            android:label="Dziekanat"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="Logowanie"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".OpeningHoursActivity"
            android:label="Godziny otwarcia"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".AuthoritiesActivity"
            android:label="Władze wydziału"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SyllabusActivity"
            android:label="Syllabus"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TimetableActivity"
            android:label="Plany Zajęć"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SkosActivity"
            android:label="SKOS"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MapActivity"></activity>
    </application>

</manifest>